import { ChangeStatusDirective } from './change-status.directive';

describe('ChangeStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeStatusDirective();
    expect(directive).toBeTruthy();
  });
});
